# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

#pooler
#DATABASE_URL="postgresql://postgres:password@localhost:5432/datacenter"

#without pooler
# keep Neon credentials secure: do not expose them to client-side code

DATABASE_URL="postgresql://neondb_owner:npg_QJ9gNtSjFBX4@ep-steep-dream-ad74elsk.c-2.us-east-1.aws.neon.tech/neondb?sslmode=verify-full"
NEXTAUTH_URL=http://localhost:3000
APP_URL=http://localhost:3000
NEXTAUTH_SECRET=BWWITySQrQZ3BPMfwlqhVVbEpknIPyb12inslkW0hmo=

# MinIO Configuration (S3-compatible storage)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=datacenter
MINIO_USE_SSL=false

# Redis Configuration
REDIS_URL=redis://localhost:6379